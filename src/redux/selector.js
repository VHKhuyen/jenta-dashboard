export const headerSelector = (state) => state.header;
export const modalSelector = (state) => state.modal;
export const rightDrawerSlice = (state) => state.rightDrawer;
export const leadsSlice = (state) => state.leads;
